// Menggunakan JavaScript untuk menambahkan kelas 'done' setelah animasi selesai
window.addEventListener('load', function () {
  const brandName = document.getElementById('brand-name');
  brandName.classList.add('done');
});
